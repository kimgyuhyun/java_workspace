package sec04.exam05;
//클래스 외부에서 메소드 호출
public class TestCar {
	//필드
	int speed;
	
	//생성자
	
	//메소드
	int getSpeed() {
		return speed;
	}
	
	void keyTurnOn() {
		System.out.println("키를 돌립니다.");
	}
	
	void run() {
		for( int i=10; i<=50; i+=10) { // i 가 50보다 작거나 같을때 실행문을 실행한뒤 i에 10씩 더함
			speed = i;
			System.out.println("달립니다.(시속:" + speed + "km/h");
		}
	}
}
